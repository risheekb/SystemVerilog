UVM example of a flat scoreboard with TLM FIFOs to store transaction
handles. The predictor is a component inside the scoreboard.

This expands the code in the Siemens UVM Basics course, module 7

Includes scripts to compile and run on Unix, Windows, and in Questa

Run good case:
	% make

Run with DUT error
	% make bad
